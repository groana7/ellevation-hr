# Ellevation Project

This project is a backend mockup of a Human Resources app.

## Developer Setup

* Run npm install

* Create the database:
  createdb ellevation-hr

* Seed the database:
  npm run seed